@{
    ViewData["Title"] = "Home Page";
}

@model HomeViewModel

<div>
        <p>Welcome, please use the form in the navigation bar to search for albums.</p>

@*        @if (Model.Errors != null)
        {
            @foreach (var error in Model.Errors)
            {
                <ul>
                    <li style="color: red">@error</li>
                </ul>               
            }
        }
        else
        {  

        }
    }*@
</div>
